/**
 * Shared page container: keeps the top nav flush with content on every screen.
 * Fluid full-width with responsive gutters — uses the whole viewport instead of
 * capping to a centered column, so all screens fill the available space.
 */
export const CONTAINER = 'w-full px-6 lg:px-10 xl:px-14'
